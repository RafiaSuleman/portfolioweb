import { animate, motion } from "framer-motion";
import { exit } from "process";

const stairAnimate={
    initial:{
        top:'0%',
    },
    animate:{
        top:'100%',
    },
    exit:{
        top:['100%','0%'],
    },
}
const Stairs = () => {
return(

)
}